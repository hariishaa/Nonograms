<Page
    x:Class="Nonograms.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nonograms.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Path=MainPage, Source={StaticResource ViewModelLocator}}">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="BurlyWood"/>
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="FontFamily" Value="ms-appx:/Resources/Fonts/PresentScript Normal.Ttf#PresentScript"/>
            <Setter Property="FontSize" Value="28"/>
        </Style>
    </Page.Resources>
    <Grid Background="{StaticResource BackgroundImage}">
        <StackPanel x:ConnectionId='1' x:Name="MainMenuStackPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="НАЧАТЬ ИГРУ" Command="{Binding NavigateCommand}"/>
            <Button Content="ОБУЧЕНИЕ"/>
            <Button x:ConnectionId='4' Content="НАСТРОЙКИ"                     />
            <Button Content="О ПРИЛОЖЕНИИ"/>
        </StackPanel>
        <Grid x:ConnectionId='2' x:Name="SettingsGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Background="BurlyWood" Opacity="0.85" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="НАСТРОЙКИ" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="ms-appx:/Resources/Fonts/PresentScript Normal.Ttf#PresentScript" FontSize="28" Margin="20"/>
            <StackPanel Grid.Row="1" Margin="20">
                <ToggleSwitch Header="Запускать во весь экран" OffContent="Выкл." OnContent="Вкл." FontFamily="ms-appx:/Resources/Fonts/HanZi.ttf#Han Zi"/>
                <ToggleSwitch Header="ПОМОЩЬ В ИГРЕ" OffContent="Нет" OnContent="Да" FontFamily="ms-appx:/Resources/Fonts/PresentScript Normal.Ttf#PresentScript" FontSize="20"/>
                <ComboBox Header="Тип метки">
                    <ComboBoxItem Content="Точка" IsSelected="True"/>
                    <ComboBoxItem Content="Крестик"/>
                </ComboBox>
                <Button x:ConnectionId='3' Content="ОК" Margin="0"                     />
            </StackPanel>
        </Grid>
    </Grid>
</Page>

