<Page
    x:Class="Nonograms.View.NonogramPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nonograms.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Nonograms.CustomControls"
    mc:Ignorable="d"
    DataContext="{Binding Path=NonogramPage, Source={StaticResource ViewModelLocator}}">
    <Grid x:ConnectionId='1' x:Name="LayoutRoot" Background="CadetBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:NonogramControl LeftSideValues="{Binding LeftSideValues}" TopSideValues="{Binding TopSideValues}" Field="{Binding Field, Mode=TwoWay}" TagType="{Binding TagType}" CheckMode="{Binding CheckMode}" IsSolved="{Binding IsSolved, Mode=TwoWay}" Width="341.3" />
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--<Button Content="click" Click="Button_Click"/>-->
            <Button Content="CheckMode" Command="{Binding CheckModeCommand}" Margin="20"/>
            <Button Content="TagMode" Grid.Column="1" Command="{Binding TagTypeCommand}" Margin="20"/>
            <Button Content="PreviousStep" Grid.Row="1" Command="{Binding PreviousStepCommand}" Margin="20"/>
            <Button Content="Clear" Grid.Row="1" Grid.Column="1" Command="{Binding ClearCommand}" Margin="20"/>
        </Grid>
    </Grid>
</Page>

